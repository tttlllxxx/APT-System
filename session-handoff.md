# 会话交接

## 当前已验证

- 现在明确可用的部分：根目录 harness 模板已加入；当前项目为空目录
- 这轮实际跑过的验证：`python3 -m json.tool feature_list.json`、`bash -n init.sh`、`./init.sh`

## 本轮改动

- 新增了哪些代码或行为：未新增业务代码
- 基础设施或 harness 发生了哪些变化：新增 AGENTS/CLAUDE 指令、进度日志、功能清单、初始化脚本、收尾检查、评审表和质量文档

## 仍损坏或未验证

- 已知缺陷：无业务代码可验证
- 未验证路径：真实安装、测试、启动命令尚未定义
- 下一轮会话需要注意的风险：不要把 `feature_list.json` 里的空列表当成项目没有需求，只是当前尚未录入

## 下一步最佳动作

- 最高优先级未完成功能：未定义
- 为什么它是下一步：需要先确定真实项目目标和技术栈
- 什么结果才算 passing：`init.sh` 替换为真实命令，`feature_list.json` 录入真实功能并带验证步骤
- 这一步中哪些东西不要动：不要恢复模板中的示例 chat 功能，除非该项目确实要做聊天应用

## 命令

- 启动命令：未定义；当前 `./init.sh` 只打印占位启动命令
- 验证命令：`./init.sh`
- 定向调试命令：未定义
